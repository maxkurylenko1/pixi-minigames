export const VIEW_W = 600;
export const VIEW_H = 350;

export const RADIUS = 22; // brush radius
export const STEP = RADIUS * 0.6; // distance between brush stamps

// coarse grid for fast progress calc (bigger -> faster, smaller -> more precise)
export const GRID_CELL = 10; // px
export const REVEAL_THRESHOLD = 0.7; // 70% scratched -> auto reveal
