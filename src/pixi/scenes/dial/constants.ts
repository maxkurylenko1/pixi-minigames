export const SECTORS = 10; // number of segments
export const OUTER_R = 180; // outer radius
export const INNER_R = 110; // inner radius (donut)
export const LABEL_R = (INNER_R + OUTER_R) / 2; // label radius
export const CENTER_Y_OFFSET = 0; // shift dial up/down if needed

export const INDICATOR_ALPHA = 0.85;
export const WHEEL_LINE = 0x2a2f3a;
export const INDICATOR_COLOR = 0x4cc9f0;

export const ROTATE_TWEEN_MS = 420;
